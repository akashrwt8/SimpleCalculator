#include <stdio.h>

int main() {
        int a, b, c;
        do
        {
            int choice;
            printf("Menu :\n");
            printf("1 Adding two numbers\n");
            printf("1 Adding two numbers\n");
            printf("1 Adding two numbers\n");
            switch (choice)
            {
            case 1:
                add()
                break;
            
            default:
                break;
            }
        } while (choice != 0);
        
    return 0;
}